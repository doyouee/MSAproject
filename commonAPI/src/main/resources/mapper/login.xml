<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper SYSTEM "../dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inzent.commonAPI.mapper.LoginMapper">
	<!-- 로그인 사용자 정보 확인 -->
	<select id="loginConfirm" parameterType="com.inzent.commonAPI.vo.UserVO">
		SELECT count(*) FROM USERS
		WHERE USER_EMAIL = #{userEmail}
		AND USER_PASSWORD = #{userPassword}
		AND DELETE_YN = 'N'
	</select>
</mapper>